# OpenAI API Key for the AI DM
OPENAI_API_KEY=your_api_key_here

# Optional: Model name (default: gpt-3.5-turbo or similar)
LLM_MODEL=gpt-4o

# Database Path (default to local holodeck.db)
DB_PATH=holodeck.db
